import * as auth from "./auth.graphql";
import * as userData from "./userData.graphql";
import * as gender from "./gender.graphql";

const typeDefs = [auth, userData, gender];
export default typeDefs;
