type Query {
  test: String
}

type Mutation {
  authUser(login: String!, password: String!, isSigned: Boolean!): AuthUserResponse!
}

type AuthUserResponse {
  accessToken: String
  refreshToken: String
}
